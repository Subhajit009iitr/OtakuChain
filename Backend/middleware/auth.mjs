import { mongo } from "mongoose";
import { imageModel } from "../db/models.mjs";
import jwt from "jsonwebtoken";

const demoMiddleWare = async (req, res, next) => {
    next()
}


export default isIOTDevice;